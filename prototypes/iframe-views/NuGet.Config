<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- No download sources: the pinned WebView2 SDK must come from the local package cache. -->
  <packageSources>
    <clear />
  </packageSources>
</configuration>
